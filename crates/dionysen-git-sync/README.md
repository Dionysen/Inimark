//! README for the Rust crate — see also packages/git-sync/README.md

GitHub / Gitee OAuth + encrypted vault (`com.dionysen.git-sync`) + PWB backup sync.

Completely isolated from `dionysen-cloud-sync` (Aliyun).
