{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only plugins (updater / process / window-state)",
  "windows": ["main", "settings"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "updater:default",
    "process:default",
    "window-state:default"
  ]
}
